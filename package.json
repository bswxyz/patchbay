{
  "name": "patchbay",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Patchbay — small-batch eurorack modules. Parable design showcase.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
